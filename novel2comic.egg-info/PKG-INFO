Metadata-Version: 2.4
Name: novel2comic
Version: 0.1.0
Summary: Pipeline to convert novel chapters into comic panels using LLM, image generation, and TTS
Author: Your Name
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: httpx>=0.27.0
Requires-Dist: fastapi>=0.110.0
Requires-Dist: uvicorn[standard]>=0.29.0
Requires-Dist: jinja2>=3.1.0
Requires-Dist: python-multipart>=0.0.6

# 小说生成漫画管线

本项目提供一个纯 Python 的命令行应用，按章节输入小说文本，协同调用 LLM、图像生成与 TTS 服务，自动生成漫画故事板、面板图片及旁白音频。

## 功能概览
- **章节解析**：自动读取指定文件夹内的 `.txt`/`.md` 章节文件。
- **场景拆解**：调用 LLM 将每章拆解为若干场景与关键视觉节拍。
- **故事板生成**：进一步生成每个场景的面板提示词与对话脚本。
- **图像生成**：调用外部 AIGC 模型生成面板图片。
- **语音合成（可选）**：对每个面板的台词生成旁白音频文件。
- **元数据输出**：输出每章对应的 JSON 描述，便于前端或排版系统消费。
- **布局排版**：依据场景自动匹配常见分镜模板，并在元数据中记录坐标。
- **自动质检（可选）**：可启用 LLM 进行内容审核，标记需人工复核的画面。
- **生成记录**：支持写入 SQLite 数据库，便于检索、回溯与版本管理。
- **Web 控制台**：内置 FastAPI 前端，可在浏览器中上传章节并查看任务状态。

## 快速开始
1. **准备章节文本**：将小说拆分为按顺序命名的 `.txt` 或 `.md` 文件，例如：
   ```
   chapters/
     01_prologue.txt
     02_chapter1.txt
   ```
2. **安装依赖**：
   ```pwsh
   python -m venv .venv
   .\.venv\Scripts\Activate.ps1
   pip install -e .
   ```
3. **配置密钥**：设置所需服务的环境变量，例如：
   ```pwsh
  $env:LLM_API_KEY="sk-your-llm-key"
  $env:IMAGE_API_KEY="sk-your-image-key"
  $env:TTS_API_KEY="sk-your-tts-key"  # 若启用语音
   ```
4. **执行管线**：
   ```pwsh
  novel2comic chapters output --with-tts --enable-quality --database .\records.sqlite

5. **启动 Web 控制台（可选）**：
  ```pwsh
  novel2comic-web
  ```
  默认在 `http://127.0.0.1:8000` 提供图形化界面，可在浏览器内上传章节文件、配置参数与查看任务进度。
   ```
   运行后将生成以下目录：
   - `output/storyboards/`
   - `output/panels/`
   - `output/audio/`
   - `output/metadata/`
  并在指定数据库中写入章节与面板记录。

## 自定义配置
通过 `--config` 指定 JSON 文件可覆盖默认模型与参数，例如：
```json
{
  "output_dir": "D:/comics",
  "llm": {
    "model": "gpt-4.1",
    "api_base": "https://openai.qiniu.com/v1",
    "temperature": 0.6
  },
  "image": {
    "model": "sdxl-lightning",
    "api_base": "https://openai.qiniu.com/v1",
    "width": 1024,
    "height": 1536
  },
  "tts": {
    "voice": "narrator",
    "model": "fast-tts",
    "speaking_rate": 1.1
  },
  "database_path": "./records.sqlite",
  "enable_quality": true
}
```
执行时使用：
```pwsh
novel2comic chapters output --config .\pipeline.json --with-tts
```
命令行参数优先级高于配置文件，可按需覆盖 `--database` 与 `--enable-quality`。

## 后续拓展建议
- 引入面板布局引擎，支持常见分镜模板、气泡排版与导出高清 PDF。
- 集成自动质检流程，例如调用 CLIP/BLIP 评分或审核 LLM，标记需人工复核的面板。
- 将生成记录与原始章节映射写入数据库/向量库，支持检索、版本管理与重复利用。
- 拓展 Web 前端或桌面 UI，提供章节上传、参数配置与实时预览的交互式工作台。
- 增加多语言支持（字幕翻译、语音多语言），满足全球化发行需求。
- 引入协作与反馈系统，让编剧、美术、编辑在同一工作区审阅并打回修改。
